# windsurf

Adds the Windsurf (Codeium) editor CLI to `$PATH`.

## What It Does

- Prepends `~/.codeium/windsurf/bin` to `$PATH`, making the `windsurf` command available.

## Prerequisites

- Windsurf editor installed with CLI tools at `~/.codeium/windsurf/`.

# local-bin

Adds the user-local binary directory to `$PATH`.

## What It Does

- Appends `~/.local/bin` to `$PATH`, making user-installed scripts and binaries available.

## Notes

This is a common location for `pip install --user` binaries and other user-local tools.

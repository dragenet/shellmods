# vim-default-editor

Sets `vim` as the default text editor.

## What It Does

- Exports `EDITOR=vim` for both local and remote (SSH) sessions.

## Notes

Mutually exclusive with `nano-default-editor`. Enable only one editor module at a time.

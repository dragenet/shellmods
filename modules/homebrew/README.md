# homebrew

Initializes Homebrew shell environment.

## What It Does

- Evaluates `brew shellenv` to set up `PATH`, `MANPATH`, `INFOPATH`, and other Homebrew-related environment variables.

## Prerequisites

- Homebrew installed at `/opt/homebrew` (Apple Silicon default). For Intel Macs, the path may need adjustment.
